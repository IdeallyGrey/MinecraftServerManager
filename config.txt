defaultMincraftPort="25565" #The default port a server should connect to. The ability to use this has not yet been implemented, so please don't touch it.
javaPath="java" #The command to execute java. If you have multiple java instances, you can specify which one to use here.
